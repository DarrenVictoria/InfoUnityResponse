# .github/workflows/deploy.yml
name: Deploy to Firebase Hosting on merge
"on":
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      # Add a step to update version for cache busting
      - name: Update Version
        run: |
          echo "VITE_APP_VERSION=$(date +'%Y.%m.%d.%H%M')" >> .env

      # Clean previous build
      - name: Clean build
        run: |
          rm -rf dist/

      - name: Build
        run: npm run build
        env:
          VITE_APP_VERSION: ${{ github.sha }}

      # Verify PWA assets
      - name: Verify PWA assets
        run: |
          if [ ! -f "dist/manifest.webmanifest" ]; then
            echo "Error: manifest.webmanifest not found"
            exit 1
          fi
          if [ ! -f "dist/registerSW.js" ]; then
            echo "Error: registerSW.js not found"
            exit 1
          fi

      # Deploy to Firebase
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_INFOUNITY_RESPONSE }}"
          channelId: live
          projectId: infounity-response

      # Add cache control headers for PWA assets
      - name: Update Firebase Hosting headers
        run: |
          echo "hosting:
            public: dist
            headers:
            - pattern: '/manifest.webmanifest'
              headers:
                - key: Cache-Control
                  value: 'no-cache'
            - pattern: '/registerSW.js'
              headers:
                - key: Cache-Control
                  value: 'no-cache'
            - pattern: '/**/*.{js,css,html}'
              headers:
                - key: Cache-Control
                  value: 'max-age=0'" > firebase.json
